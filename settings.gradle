pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Aptoide"
// Keep it in alphabetical order
include ':app',
        ':features:home',
        ':libraries:arch',
        ':libraries:design',
        ':libraries:navigation'
